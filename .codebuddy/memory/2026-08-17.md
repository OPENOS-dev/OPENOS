# 2026-08-17

## src/ 全部 26 个组件子模块化（完成）
- **用户决策**：`src/` 下所有模块都作为子模块；`opt` 合并方式 = 把 `openos-opt`（QML app）**原样移动**进 `opt/openos-opt/`（CLI 在 `opt/` 根下，`opt.sh` 不改），一个 `OPENOS-opt` 仓库承载两者。
- 已解压 `bash/src/bash-5.3.tar.gz` → `bash/src/bash-5.3/`（bash 此前只有 tarball；coreutils 早有解压目录）。
- 每个子模块：`.gitignore`（`.DS_Store`；coreutils/bash 额外 `src/*.tar.gz*` 排除下载包，**保留**测试套件内数据压缩包如 gcc/util-linux/systemd 的）→ git init -b main → commit → remote add origin。
- 26 个仓库全部在 `https://github.com/OPENOS-dev/OPENOS-<name>.git`（大小写注意：`OPENOS-OAK`、`OPENOS-OPENUI-desktop`、`OPENOS-liboak` 等）。

## 关键教训：主仓库根在项目根，不在 OPENOS_CORE
- 主仓库 `.git` 在 `/Users/cangcang/SYSTEM-project/OPENOS- PROJECT/`（项目根），`OPENOS_CORE/` 只是仓库内目录；在 `OPENOS_CORE/` 里跑 git 命令操作的是项目根仓库。
- **`.gitmodules` 必须放仓库根**，且 `path` 用完整前缀 `OPENOS_CORE/src/<name>`（否则 `git submodule status` 报 "no submodule mapping"）。
- 已修正：根 `.gitmodules` 26 条 + 索引 gitlink 26 个（git submodule status 全部匹配）。
- 主仓库提交：`649a847`（含 0c892b2 初始 + aff87f2 忽略 .DS_Store + 649a847 移动 .gitmodules）。根 `.gitignore` = `.DS_Store`。
- `OPENUI-desktop` 曾因早期被 add 为普通文件（100644），`git rm --cached -r` 后重挂为 gitlink。

## 子模块提交记录（各组件 DEV2026.1）
- 主仓库 649a847；kernel 2219c1e（343469 文件）；gcc 19f82c1（158919）；glibc 95d4667（20944）；systemd 446e326；coreutils e5122f0（3795）；util-linux f5c94b0（3710）；bash 04523e1（1613）；apt 24ee9dd（892）；pacman ceb9d68（840）；linux 698782d；liboak 1db92b7；libopenrsa ff38217；libvmapp 1d4db5e；oakctl 947a102；openos-oak-seal ba35375；openos-uname ff2d955；openos-settings a8aaaf4；openos-calendar 32c7ce8；openos-welcome b3d63de；openos-vmapp 3b0f6ca；openos-settingsd fc89508；openos-securityd ae3db29；openos-run 7275152；openos-oak 06753d4；opt 9a368d9；OPENUI-desktop 810fd8f。

## 子模块 CI + 共享 binpkg 仓库（完成）
- **机制**：每个子模块 `.github/workflows/build.yml` 自构建（5 架构矩阵 x86-64/x86/arm64/arm32/loong64），产出 `<组件>-<架构>-DEV2026.1.pkg.tar.zst` 推送到共享仓库 `OPENOS-BUILD-BINPKG`（用户建，空仓库已初始化，含 README/OPL LICENSE）。
- **共享仓库**：`https://github.com/OPENOS-dev/OPENOS-BUILD-BINPKG.git`，目录 `bins/<组件>/<架构>/`。
- **推送认证**：各子模块仓库 secret `OPENOS_BINPKG_TOKEN` = gh auth token（账号 cangcang-zh-2025，repo/workflow 权限），已全部配置（26 仓库）。workflow 无 token 时跳过推送（warning）。
- **构建类型**：自研组件/Qt app=meson；apt=cmake；bash/coreutils/util-linux/pacman=autotools；glibc=cross configure；kernel=四线 defconfig+SECURITY 强制；gcc/systemd=预留空包；linux=打包配方归档。
- **主系统 CI**：`build.yml` 改为 `fetch_binpkgs.sh --arch`（克隆 binpkg 仓库→解压组件到 sysroot/内核到 kernels）+ `build_image.sh`，不再本地构建。新增 `BUILD_SYSTEM/fetch_binpkgs.sh`，`build_image.sh` 支持内核 binpkg `boot/` 布局。
- **踩坑**：
  - Ubuntu 24.04 无 `gcc-loongarch64-linux-gnu` meta 包 → 用 `gcc-13-loongarch64-linux-gnu g++-13-loongarch64-linux-gnu`（loong64 交叉编译器）。
  - 生成脚本双引号 heredoc 中 `$WORK` 提前展开导致 `set -u` 报错 → `\$WORK` 转义。
  - `stage()` 幂等不适合 fetch（每次需重新拉最新）→ fetch 用普通命令。
  - gh secret set 网络 EOF/404 误报 → 重试 + `gh secret list` 复核；`openos-run`→`OPENOS-run`、`openos-oak-seal`→`OPENOS-oak-seal`、`openos-uname`→`OPENOS-uname`（用户仓库名无 `openos-` 前缀）。
- **CI 状态（已端到端跑通）**：binpkg 仓库已见 46+ 产物（libvmapp 5 架构全、systemd、openos-run、openos-vmapp、openos-oak-seal 等）。
- **第二轮修复**：
  1. **meson 不支持 'bash' 语言**：`openos-uname`/`oakctl`/`opt` 的 meson.build `project(...,'bash')` 报错 → 改为无语言 `project('<name>')`（纯 install_data）。
  2. loong64 交叉包名已修（gcc-13-loongarch64-linux-gnu），新 push run 生效。

## 失败上报 + 自动 Issue 链路（用户需求，完成并验证）
- **子模块失败上报**：CI 构建 step `exec > >(tee "$RUNNER_TEMP/build.log") 2>&1` 全程记日志；失败时 `if: failure()` step 把失败原因（仓库/运行链接/提交/时间戳/日志尾部 80 行）写入 binpkg 仓库 `logs/<组件>/<架构>.log`（先 `rm -f` 删旧再写新）。
- **主仓库聚合**：`.github/workflows/failure-report.yml`（每小时 cron `30 * * * *` + workflow_dispatch）克隆 binpkg 仓库读 `logs/*/*.log`，用 `gh issue create`（github.token，permissions: issues: write）自动创建 `[CI-FAIL] <组件> (<架构>) 构建失败` Issue（同标题 open issue 去重跳过）+ `[CI-FAIL] 构建失败汇总`（更新式）。
- **已验证**：binpkg 仓库出现真实失败 log；手动触发 failure-report 自动创建了 6 个 Issue（kernel×2/liboak/oak-seal/oak/汇总），内容含日志。
- **踩坑**：
  - 上报 step 忘 cd → log 写到仓库根目录（arm32.log 等，组件互相覆盖）；修复为 `logs/${COMPONENT}/$ARCH.log` 完整路径，并手动清理重排过一次（pull-rebase-push 循环抗并发）。
  - binpkg 仓库被各 CI 并发 push → push 前必须 `git pull --rebase` 循环重试。

## 第三轮：构建失败根因修复（已完成，CI 重新触发）
| 组件 | 根因 | 修复 |
|---|---|---|
| kernel (arm32/x86-64) | `scripts/module.lds`/`usr/initramfs_data.o` 缺失 | 构建顺序：defconfig → olddefconfig → **`prepare scripts_basic`**（先建 host 工具/module.lds）→ all |
| liboak (arm64) | `libsodium` 依赖缺失 | apt 依赖加 `libsodium-dev`（所有 workflow） |
| pacman (x86) | 实为 meson 项目，误配 autotools → `./configure: No such file` | 改 `pacman_steps` meson 构建（src/pacman.git） |
| coreutils (loong64) | `./configure` 缺失（日志被杂散覆盖，待新 run 确认） | 重触发验证 |
- 已批量提交推送 26 子模块 + 同步主仓库指针（5f3fb78）。

## 待办
- 等待第三轮 CI 完成，验证失败已修复、binpkg 全组件产物齐全。
- 验证主仓库 assemble（fetch_binpkgs + build_image）产出镜像。
- gcc/systemd workflow 仍为预留空包。
