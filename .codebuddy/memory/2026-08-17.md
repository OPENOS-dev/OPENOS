# 2026-08-17

## src/ 全部 26 个组件子模块化（完成）
- **用户决策**：`src/` 下所有模块都作为子模块；`opt` 合并方式 = 把 `openos-opt`（QML app）**原样移动**进 `opt/openos-opt/`（CLI 在 `opt/` 根下，`opt.sh` 不改），一个 `OPENOS-opt` 仓库承载两者。
- 已解压 `bash/src/bash-5.3.tar.gz` → `bash/src/bash-5.3/`（bash 此前只有 tarball；coreutils 早有解压目录）。
- 每个子模块：`.gitignore`（`.DS_Store`；coreutils/bash 额外 `src/*.tar.gz*` 排除下载包，**保留**测试套件内数据压缩包如 gcc/util-linux/systemd 的）→ git init -b main → commit → remote add origin。
- 26 个仓库全部在 `https://github.com/OPENOS-dev/OPENOS-<name>.git`（大小写注意：`OPENOS-OAK`、`OPENOS-OPENUI-desktop`、`OPENOS-liboak` 等）。

## 关键教训：主仓库根在项目根，不在 OPENOS_CORE
- 主仓库 `.git` 在 `/Users/cangcang/SYSTEM-project/OPENOS- PROJECT/`（项目根），`OPENOS_CORE/` 只是仓库内目录；在 `OPENOS_CORE/` 里跑 git 命令操作的是项目根仓库。
- **`.gitmodules` 必须放仓库根**，且 `path` 用完整前缀 `OPENOS_CORE/src/<name>`（否则 `git submodule status` 报 "no submodule mapping"）。
- 已修正：根 `.gitmodules` 26 条 + 索引 gitlink 26 个（git submodule status 全部匹配）。
- 主仓库提交：`649a847`（含 0c892b2 初始 + aff87f2 忽略 .DS_Store + 649a847 移动 .gitmodules）。根 `.gitignore` = `.DS_Store`。
- `OPENUI-desktop` 曾因早期被 add 为普通文件（100644），`git rm --cached -r` 后重挂为 gitlink。

## 子模块提交记录（各组件 DEV2026.1）
- 主仓库 649a847；kernel 2219c1e（343469 文件）；gcc 19f82c1（158919）；glibc 95d4667（20944）；systemd 446e326；coreutils e5122f0（3795）；util-linux f5c94b0（3710）；bash 04523e1（1613）；apt 24ee9dd（892）；pacman ceb9d68（840）；linux 698782d；liboak 1db92b7；libopenrsa ff38217；libvmapp 1d4db5e；oakctl 947a102；openos-oak-seal ba35375；openos-uname ff2d955；openos-settings a8aaaf4；openos-calendar 32c7ce8；openos-welcome b3d63de；openos-vmapp 3b0f6ca；openos-settingsd fc89508；openos-securityd ae3db29；openos-run 7275152；openos-oak 06753d4；opt 9a368d9；OPENUI-desktop 810fd8f。

## 待办（已全部完成）
- **推送完成（HTTPS token，osxkeychain 凭据有效）**：26 个子模块 + 主仓库全部推送到 GitHub。
- 主仓库远程原有 6 条独立历史（LICENSE/EULA/README），本地用 `git merge origin/main --allow-unrelated-histories` 合并（无冲突），推送成功。

## 推送技巧（大仓库/SSL 抖动）
- 大仓库（gcc 1.2G）推送断连：`git -c http.postBuffer=2097152000 push`。
- "Server aborted the SSL handshake" 瞬时错误：`GIT_HTTP_LOW_SPEED_LIMIT=1000 GIT_HTTP_LOW_SPEED_TIME=90` + `-c http.version=HTTP/1.1` + 循环重试（glibc/主仓库均 1-2 次成功）。
- `GIT_TERMINAL_PROMPT=0` 防止 push 挂起等待输入。

## 最终状态
- 主仓库 `c44e533`（本地 3 提交 + 合并远程 LICENSE/EULA/README + memory 提交），`git submodule status` 26 个全部匹配。
- 推送失败记录：gcc（1 次断连后成功）、glibc（SSL 2 次后成功）、主仓库（SSL + non-fast-forward 合并后成功）。
