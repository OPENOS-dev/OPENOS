# 2026-08-16

## OPENOS 桌面增强：动态模糊 / 多工作区 / 通知层（已完成）
- 在既有 Wayland(wlroots) 合成器基础上新增三功能：
  1. **多工作区**：`compositor.c` 增加 `workspaces[4]` 场景子树 + `switch_workspace()`；`Ctrl+1..4` / `Ctrl+Tab` 切换，窗口按创建时工作区挂载并自动聚焦。
  2. **动态模糊**：新增 `desktop/src/blur.{c,h}`，两阶段渲染管线（build_state 渲染 → 读回 → CPU 高斯模糊 → 二次合成），best-effort 回退普通提交；面板/菜单/通知挂 `blur_tree`。
  3. **通知层**：新增 `desktop/src/notifyd.c`（layer-shell overlay + FIFO `/tmp/openos-notifyd.fifo` + cairo，6s 过期/点击消除）。
- `meson.build` 新增 blur.c 与 notifyd 可执行；README 更新功能清单。
- 环境说明：macOS 无 wlroots/wayland 头，lint 报错均为缺头文件所致，无法在此编译，需在 OPENOS/Linux 验证。
- 已核实 wlroots 0.17 API 关键点（wlr_scene_output_build_state 存在、wlr_scene_render_output 不存在、scene_buffer.texture 私有），细节写入 MEMORY.md。

## 第二波：任务栏 + 工作区切换器 UI + OPENUI 设计（已完成）
- 任务栏窗口列表：标准 `wlr-foreign-toplevel-management` 协议（XML 拉自 GitLab master）；panel 维护窗口链表，左键激活/右键关闭。
- 工作区切换器：**自研** `openos-workspace-unstable-v1.xml`（wlroots 无 workspace 协议），合成器端实现 `src/workspace.c`（手写 wl_global/request/event），panel 显示 4 个胶囊指示器点击切换。
- OPENUI 设计语言：`desktop/OPENUI.md` + `src/openui.h`，参考 MD3 扩展 NUI2（语义色彩/状态层叠/排印/形状/动效时长与缓动/间距/对比度/触控目标）。
- panel.c 全面重写（OPENUI 令牌 + 布局：品牌/工作区/任务栏/时钟/Menu），compositor.c 增加 ft_handle 生命周期与 request 处理，meson.build 增加两协议生成。
- 网络发现：wlroots 仓库在 GitLab；GitHub raw 对 swaywm/wlroots 的 0.17.0 tag 404（用 master 成功）。
- 限制依旧：macOS 缺 wlroots/wayland/cairo/linux 头，lint 全是缺头错误，无法本地编译验证，需在 OPENOS/Linux 构建。

## 项目结构重组 + 内核下载（用户操作，非脚本）
- 用户把 `Arch_System_Sources/` 重组为 **`OPENOS_CORE/`**：组件在 `OPENOS_CORE/src/<pkg>/`，内核在 `OPENOS_CORE/src/kernel/`，新增空骨架 `BUILD_SYSTEM/`、`LICENSES/`、`SCRIPT/`。原目录已不存在。
- 用户重新下载内核：`kernel/` 现有 `linux-6.12.103`（LTS）+ `linux-7.2-rc7`（各带 tarball）。
- **内核版本分层（用户确立）**：基础版=6.12(LTS)+7.1；当年预发布=7.2(rc→正式)；经典版=5.15(老设备，用户稍后下载)。7.1/5.15 待下载。
- **四线内核最终就绪（已核实 Makefile 版本 + 顶层结构）**：5.15.215(经典,1.2G)、6.12.103(基础 LTS,1.6G)、7.1.8(基础,1.7G)、7.2-rc7(预发布,1.7G)。均含 tarball，位于 `OPENOS_CORE/src/kernel/`。

## 构建体系 BUILD_SYSTEM（借鉴 Chromium OS，已完成）
- 用户要求"学习 Chrome OS 开源版构建系统"，据此实现 Portage/ebuild 模型映射（board/sysroot/stage/binpkg/build_image）。
- 产出：`OPENOS_CORE/BUILD_SYSTEM/{config,common,setup_board,build_component,build_image,cache}.sh` + `README.md` + `.github/workflows/build.yml`（4 架构矩阵、ccache/binpkg 缓存、minimal 默认、timeout 330）。
- 用户拒绝了我执行 chmod + bash -n 语法检查（命令被拒，未重试）；脚本未经本机验证，需 Linux/CI 跑。细节已入 MEMORY.md。

## 桌面外壳 Qt 化（用户决策：站在巨人肩膀上）
- 决策：外壳 toolkit → **Qt 6 / Qt Quick (QML)**，合成器**保留 wlroots**（KDE Plasma 式架构）。
- 新目录 `OPENUI-desktop/src/shell/`：`main.cpp`（注入 shell/wayland/OpenUI 到 QML 上下文）、`shellbackend.{h,cpp}`（窗口/工作区/通知模型 + launchApp）、`waylandbridge.{h,cpp}`（协议桥**骨架**，待 Linux 用 qtwaylandscanner+QWaylandClientExtensionTemplate 或 LayerShellQt 接线）、`qml/{OpenUI,Panel,Launcher,Notifications,main}.qml` + `qml.qrc`。
- OpenUI.qml 用 context 属性注入（去掉 pragma Singleton，避免 qmldir 复杂度）；main.cpp 实例化 OpenUI 组件注入。
- meson.build：project 改 `'c','cpp'`(cpp_std=c++17)，新增 `qt6.preprocess`(moc+qrc) + `openos-shell`；**openos-panel/notifyd 不再编译**（cairo 客户端转死代码，保留源码）。
- CI workflow：apt 增加 `qt6-base-dev qt6-declarative-dev qt6-wayland-dev qt6-wayland`。
- 已知未验证：Qt/Wayland 相关代码 macOS 无法编译，需 OPENOS/Linux 验证。

## WaylandBridge 协议接线（完成，待 Linux 编译验证）
- **决策**：数据协议（任务栏/工作区）用**原生 libwayland-client**（`wl_display_connect` + registry 绑定 + QSocketNotifier 监听 fd 集成 Qt 事件循环）；面板 layer 层用 **LayerShellQt（KDE）** 可选依赖（meson `layer_shell_qt`，found 时 `-DOPENOS_USE_LAYERSHELLQT`）。
- waylandbridge.cpp 实现：绑定 `zwlr_foreign_toplevel_manager_v1`（title/app_id/state/closed/parent listener；activate/close 请求）、`openos_workspace_manager_v1`（name/state/done；activate 请求）、`wl_seat`（activate 用）；6 路信号 windowAdded/Updated/Removed、workspaceAdded/Updated/Activated 驱动 ShellBackend 模型。
- ShellBackend 增补：`updateWindow`（ToplevelModel::update）、`setWorkspaceName`（WorkspaceModel::setName）。
- 踩坑修正：① 自由回调函数访问私有 m_impl → 加 public `Impl* impl()` getter（friend 方案繁琐弃用）；② `setWindowLayerSurface` 非 Q_INVOKABLE，改由 main.cpp 在 engine.load 后 `qobject_cast<QWindow*>` 调用，QML 移除 Component.onCompleted 无效调用；③ ws_name 应 emit workspaceUpdated 而非重复 workspaceAdded。
- meson：openos-shell 依赖加 `wayland_client` + 生成头(ft_cli/ft_code/ws_cli/ws_code) + `layer_shell_qt`；cpp_args 条件宏。
- main.qml：面板窗口保持 FramelessWindowHint 置顶（LayerShellQt 生效前兜底）。

## OAK LSM 内核安全模块（四内核，已完成）+ 强制启用（用户要求）
- 需求：`security/openos/oak_lsm.c` 注册 `security_task_kill` 钩子，拦截对受保护进程的 SIGKILL/SIGTERM，仅 Security Watchdog 可终止，否则 -EPERM。
- 固定逻辑：三个内置角色槽位（system/opt/application 守护进程），规则固定不可删，PID 由 `/proc/oak/builtin` 登记（watchdog 在守护进程就绪后写入）。
- 动态接口：白名单 `hlist_head` 哈希表（8bit 桶+spinlock），`/proc/oak/whitelist` add/del；`/proc/oak/watchdog` 登记 Security Watchdog；proc 写需 CAP_SYS_ADMIN。
- 版本兼容（已逐一核对四内核头文件）：`security_add_hooks` 第三参 5.15=char*/6.12+=lsm_id；DEFINE_LSM 5.15/6.12=`.name`/7.x=`.id`；task_kill 签名四内核一致=`const struct cred *cred`。
- **强制启用（用户"必须启用,不允许修改"）三层保证**：
  1. Kconfig `def_bool y`（无 =n 选项，SECURITY=y 即编译进内核）
  2. LSM order 强制：5.15 用 `LSM_ORDER_FIRST`（其 enum 仅 FIRST/MUTABLE，FIRST 是唯一强制项）；6.12/7.1/7.2 用 `LSM_ORDER_LAST`（FIRST/LAST 均强制，无视 lsm= 参数与 CONFIG_LSM）
  3. 构建体系 `build_component.sh` kernel-config 阶段 `scripts/config -e SECURITY -e SECURITY_OAK` + 校验 CONFIG_SECURITY_OAK=y；kernel-build 后校验 System.map 含 `oak_task_kill|__lsm_oak`，缺失即 die。
- 四内核文件完全一致（md5 e018067e...），含 Makefile/Kconfig 挂载（security/Makefile += openos/，Kconfig source）。
- 启用：CONFIG_SECURITY=y 自动启用 OAK（无需 lsm= 参数）。

## OAK 内核权利（授权提权，用户"给OAK内核权利"）
- 机制：`security_capable` 钩子不可用于提权（LSM 间 AND 语义，capability 模块先拒绝即拒绝）→ 改用**授权接口 + 直接提权**：受保护进程启动后写 `/proc/oak/authorize` token 自证，OAK 校验 PID 在保护名单后 `prepare_creds + cap_raise + commit_creds` 提权。
- 能力集合（固定逻辑，u64 位图）：watchdog=全权(SYS_ADMIN/SYS_BOOT/KILL/MKNOD/SYS_RESOURCE/DAC_OVERRIDE/CHOWN/FOWNER/SETUID/SETGID/NET_ADMIN/LINUX_IMMUTABLE/SYS_RAWIO)；system=看门狗(SYS_ADMIN/SYS_BOOT/KILL)；opt=包管理(+DAC_OVERRIDE/CHOWN/FOWNER/SETUID/SETGID/MKNOD/LINUX_IMMUTABLE)；application=生命周期(KILL/SYS_RESOURCE/DAC_OVERRIDE)；白名单默认受限，`add <pid> <hexmask>` 可覆盖（白名单条目新增 capmask 字段，`oak_wl_lookup`）。
- 关键兼容坑：`cap_last_cap()` 四内核都**没有**，用 `CAP_LAST_CAP` 宏（uapi，四内核均有）；`kernel_cap_t` 5.15=数组式/6.12+=位图式 → 统一 `cap_raise` 宏提权。
- authorize 权限 0200 只写；其余 watchdog/builtin/whitelist 写需 CAP_SYS_ADMIN。
- 四内核 md5 一致 eab203a4...（最终）。

## OAK 子安全主体 + 非对称身份握手（用户需求）
- 需求：三个内置守护 + 第三方 都作为 OAK 下的**子安全主体**（运维便利）；应用收到请求可加入子主体；身份验证用"类非对称加密"（哈希序列串=密钥对，公钥公开，双向签名验证）。
- 已实现（oak_lsm.c 扩展至 1106 行，四内核 md5 一致 15868599...）：
  - **子安全主体注册表**：`struct oak_subject{name,kind(内置/第三方),pid,capmask,pubkey,指纹}`，按 name 哈希（自实现 `oak_str_hash` FNV-1a，内核无 hash_string）。
  - 接口：`/proc/oak/subjects`（register/pubkey/unregister + 列表含指纹）、`/proc/oak/handshake`（challenge/verify）、`/proc/oak/authorize` 支持子主体名（应用加入后获权利）。
  - 内置守护经 `/proc/oak/builtin` 登记时自动成为内置子主体（kind=BUILTIN + 角色权利）。
  - **握手**（简化 STS）：双方密钥对、pub 公开注册、双向 challenge 签名验证、会话密钥 K=派生(pub_a,pub_b)。内核侧挑战生成 get_random_bytes + 会话记录（oak_sessions）+ 派生（占位），验签为**骨架**（占位校验），生产须 CONFIG_CRYPTO_RSA akcipher 替换。
  - 公钥指纹 oak_fingerprint（32 字节占位哈希，正式 crypto_shash SHA-256）。
- 文档：`OPENOS_CORE/SECURITY-OAK.md`（架构/接口/握手协议/固定vs动态/后续加固清单）。
- 说明：验签/摘要目前占位（可编译），真正非对称验证待接内核 crypto API + 用户态 oakctl。

## OAK 密钥 .oak 文件存储（用户约定）
- 约定：**所有密钥（公钥+私钥）统一用 `.oak` 文件存储**，公开放在 `OPENOS_CORE/KEYS/`。
- 结构：`KEYS/{oak-trust,oak-watchdog,system,opt,application}.oak`（公钥占位模板）+ `KEYS/.private/*.key.oak`（私钥，空占位）。
- `.oak` 格式（文本键值）：type/name/purpose/algorithm/public(Base64 或 DER hex)/fingerprint；`oak-trust.oak` 头有完整字段说明。
- 应用密钥：应用自己的安全目录存 `<app-id>.oak`(公钥)+`.private/<app-id>.key.oak`(私钥)。
- 安全边界：公钥公开进镜像，私钥 .private 受保护 + `.gitignore`(KEYS/.gitignore 排除 .private/ 和 *.key.oak) 不进版本库/镜像。
- 工具：`SCRIPT/oakctl.sh`（genkey/parse/pubkey-hex/fingerprint，openssl 生成 rsa-4096/2048/ed25519）。
- SECURITY-OAK.md 新增 §1.5 密钥存储章节。
- 当前 .oak 均为占位模板（public=BASE64/hex 占位），真密钥由 oakctl 生成。

## 命名修正：模块=OPENOS Security，OAK=加密方法（用户明确）
- 用户指出：OAK 实际上是**加密方法**；系统内的**模块**叫 **OPENOS Security**。
- 改动：config 名 `SECURITY_OAK`→`SECURITY_OPENOS`（四内核 openos/Makefile、security/Makefile、Kconfig）；LSM 名 `"oak"`→`"openos_security"`；LSM_ID_OAK→LSM_ID_OPENOS_SECURITY(=200)；日志 "OAK LSM"→"OPENOS Security"；build_component.sh 校验同步。
- **保留 OAK**：`oak_` 前缀函数/`/proc/oak/`/`KEYS/*.oak`/`oakctl.sh`/握手协议 —— OAK 仍是模块内部的加密方法名。
- 修复：6.12 的 security/openos/Makefile 之前丢失（已重建）；四内核 oak_lsm.c md5 一致 f3a81976...。
- 文档 SECURITY-OAK.md 开头加命名说明；MEMORY.md 加命名规范条目。

## OAK-Seal 启动完整性校验（openos_oak_early_init，已完成）
- 需求：start_kernel 阶段经 `openos_oak_early_init()` 从硬盘偏移 0x1000 读 OAK-Seal 封印，crypto_shash 算内核 SHA-256 比对，失败 machine_halt() 锁死；用户可输入用户名+密码解锁。仅依赖 blkdev_get_by_path + crypto_shash。
- 新文件 `security/openos/oak_early.c`（四内核一致 md5 3038563e...）：
  - 封印块结构 oak_seal_block（magic "OAKS" + kernel_hash + user_hash + pass_hash）。
  - 跨版本读块：5.15=blkdev_get_by_path(FMODE_READ)+read_mapping_page+kmap_local_page；6.12/7.x=bdev_file_open_by_path(BLK_OPEN_READ,NULL,NULL)+kernel_read（已核实签名 4 参）。
  - 哈希目标 _text.._end；crypto_alloc_shash("sha256")+SHASH_DESC_ON_STACK。
  - 解锁：early_param oak.seal_dev/oak.user/oak.pass(密码sha256hex)/oak.strict；失败比对 user_hash+pass_hash，匹配则降级放行，否则 machine_halt。
  - 早期子系统未就绪时：strict=1 停机，否则警告降级（防误锁正常启动）。
  - 坑：hex2int_hi/lo 不存在，用 hex_to_bin（5.15 kernel.h / 6.12+ hex.h 条件 include）；blkdev_get_by_path 6.x 移除改用 bdev_file_open_by_path。
- 四内核 init/main.c start_kernel 在 rest_init()(5.15 arch_call_rest_init()) 前插入 extern+调用（1071/1072、1153/1154 等）。
- openos/Makefile: obj-$(CONFIG_SECURITY_OPENOS) += oak_lsm.o oak_early.o。
- build_component.sh System.map 校验加 openos_oak_early_init。
- 交互输入说明：start_kernel 无 tty，用户名+密码由引导器/initramfs 询问后经 cmdline 传递。

## 用户态 OAK 守护进程 openos-securityd（C，已完成）
- 需求：C 编写用户态原型（先用户态后下沉内核）——监听 Unix Socket `/run/openos/oak.sock`，收 (时间戳,命令,哈希H1)，用本地 OAK-SK 算 H2=SHA256(H1+OAK-SK)，比对返回 OK/DENY，libsodium/OpenSSL 算哈希，完整 main 循环 + 信号处理。
- 文件：`SCRIPT/openos-securityd.c`（libsodium crypto_hash_sha256；poll 主循环 + SIGTERM/INT/QUIT 优雅退出 + SIGPIPE 忽略；时间戳防重放 NONCE_SLACK=300 + 命令白名单 seal-verify/oak-handshake/oak-whitelist；socket 目录 0700/socket 0660；OAK-SK 从 KEYS/.private/oak-sk.key 读取，路径可用 argv 覆盖 + 原型回退路径）。
- 配套：`SCRIPT/build-securityd.sh`（cc -lsodium）、`SCRIPT/oak-test-client.c`（测试客户端，同算法生成 H1/H2）、`KEYS/.private/oak-sk.key` 占位（chmod 600，.gitignore 增加 *.key 和 oak-sk.key）。
- SECURITY-OAK.md 增 §4.5。
- 说明：原型在 OPENOS/Linux 上运行（需 libsodium）；macOS 未编译验证。协议行格式 `<ts> <cmd> <H1_hex>`，响应 `OK <H2>` / `DENY`。

## OPEN RSA 握手协议（纯逻辑，C，已完成）
- 需求：`openrsa_handshake()` 核心函数——客户端私钥签名生成 H1，服务端私钥叠加生成 H2，验签用客户端公钥；时间戳毫秒级（clock_gettime）；附单元测试。
- 文件：`SCRIPT/openrsa.{c,h}`（RSA-2048+SHA-256 PSS，openssl EVP；`openrsa_ms()`=CLOCK_REALTIME ns→ms；`openrsa_client_sign()` 客户端签名 H1；`openrsa_handshake()` 服务端验签 H1(客户端公钥)+叠加签名 H2）、`SCRIPT/openrsa_test.c`（6 用例）、`SCRIPT/build-openrsa.sh`。
- 协议：H1=sign(privC, sha256(data||ts_ms))；H2=sign(privS, sha256(H1||ts_ms))；验签 pubC。
- 返回码：0 成功 / -1 参数错误 / -2 验签失败 / -3 服务端签名失败。
- 单元测试用例：①正常握手 ②篡改数据拒绝 ③攻击者伪造 H1 拒绝 ④篡改 H1 拒绝 ⑤毫秒时间戳单调 ⑥参数校验。
- SECURITY-OAK.md 增 §4.6。需 openssl 编译（macOS 未验证）。

## OAK-Seal 封印工具 openos-oak-seal（用户态写硬盘头，已完成）
- 需求：新内核安装后重新封印硬盘——读 /boot/vmlinuz-$(uname -r) 算 SHA-256；用环境变量 OAK-SK 派生临时密钥加密 LUKS 主密钥（dm-crypt keyring）；写 /dev/sdX 偏移 0x1000（需确认设备名）；校验写入；尽量静态链接 OpenSSL。
- 文件：`SCRIPT/openos-oak-seal.c` + `SCRIPT/build-oak-seal.sh`（优先 -static 回退动态）。
- 封印块布局严格对齐内核 oak_early.c struct oak_seal_block（magic "OAKS"/version/kernel_len/kernel_hash/user_hash/pass_hash/reserved，显式 LE）。
- 实现：HKDF-SHA256（HMAC 实现）派生临时密钥；AES-256-CTR 加密 LUKS 密钥（reserved 区 iv[16]+len[4]+密文）；DM_TABLE_STATUS ioctl 优先 + dmsetup --showkeys 回退取密钥；设备二次确认。
- 用法：`OPENOS_OAK_SK=... ./openos-oak-seal /dev/sda <dm> <user> <pass>`。
- SECURITY-OAK.md 增 §4.7。未在 macOS 编译验证。

## Qt + wlroots 胶水桥（OAK 状态指示器，已完成）
- 需求：wlr_output_frame 中 QSGRenderer 渲染 QML；QML OAKState 颜色与 openos-securityd socket 消息绑定；main.cpp 集成 Qt 事件循环与 wlroots 主循环；C++17。
- 目录：`OPENUI-desktop/src/shell/oakindicator/`：
  - `oak_bridge.{h,cpp}`：OakSocketClient（Unix socket /run/openos/oak.sock，QSocketNotifier 监听，OK/DENY→NUI2 状态色，start() 未连上置 offline 警告色）+ OakQmlRenderer（QQuickRenderControl 离屏渲染→grabWindow→wlr_texture_from_pixels 上传）。
  - `OAKState.qml`：状态胶囊（objectName=oakState），color 属性由 C++ setProperty 绑定。
  - `oak_mainloop.{h,cpp}`：单线程轮询（processEvents + wl_display_dispatch_pending/阻塞 dispatch 防忙轮询）/ QThread 线程分离双模式。
  - `main.cpp`：合成器集成框架（atomic quit + perFrame 回调渲染上传）。
  - `CMakeLists.txt`：C++17 + Qt6 Quick/Qml + wlroots pkg-config；`qml_resources.qrc`。
- SECURITY-OAK.md 增 §4.8。需 Linux（wlroots）编译验证，macOS 不可。

## OPENOS Settings API（OAK 加密通信 + 自动创建守护 + 用户授权，已完成）
- 需求：应用通过 OAK 加密与 Security 模块通信；自动创建设置守护；应用添加前先询问用户是否允许。
- 架构：App → liboak(OAK 加密 H2=SHA256(H1+SK)) → openos-settingsd(自动 fork/exec 拉起) → 授权确认(弹通知→允许/拒绝→持久化 /var/lib/openos/authorized.conf) → /proc/oak/* → 内核。
- 文件：
  - `SCRIPT/liboak.{h,c}`：API 层（oak_settings_request/oak_authorized/oak_set_sk_path；状态码 OAK_OK/DENIED/PENDING/ERR_*；类别 WATCHDOG/BUILTIN/WHITELIST/SUBJECT/UNLOCK；ensure_settingsd 自动拉起）。
  - `SCRIPT/openos-settingsd.c`：设置守护（监听 /run/openos/settingsd.sock；verify_oak 重算比对 H2；app_authorized/grant_app 持久化；ask_user 弹通知；forward_to_kernel 写 /proc/oak）。坑：struct oak_settings_req_parsed 需顶部前向声明（已修），补 stdarg.h。
  - `SCRIPT/build-oak-settings.sh`（liboak.a + settingsd）、`OAK-SETTINGS-API.md`（架构/API/流程）。
- 协议：AUTH <app_id> → GRANTED/PENDING/DENIED；SET <kind> <target> <value> <app_id> <H2> → OK/PENDING/DENIED。
- 原型说明：ask_user 目前打印+默认允许（演示），生产接桌面通知+按钮确认。
- lint 错误全为 macOS 缺 libsodium/openssl/linux 头，需 Linux 编译验证。

## vmapp 轻量软件隔离（内核，四内核，已完成）
- 需求：`vmappapi(app_name, enable, sub_path)` 系统调用——opt 调用时自动获得独立 /vmapp/<app> 根视图；应用抽屉列出 .desktop；普通应用默认不可见。
- 内核实现 `security/openos/vmapp.c`（四内核一致 md5 afbf5d8a...）：字符设备 `/dev/vmapp` + ioctl（避免四内核×四架构 syscall 表改动），用户态 `libvmapp` 提供同名 `vmappapi()`。
- 关键实现：vmapp_check_name 防路径遍历；vmapp_mkdir_path（**kern_path_create 四参跨版本一致 + vfs_mkdir 跨版本签名差异**：5.15=init_user_ns/6.12+=nop_mnt_idmap 条件编译）；enable=1 时 ksys_unshare(CLONE_NEWNS)+set_fs_root/set_fs_pwd 切视图；readdir 用 iterate_dir+dir_emit 过滤隐藏文件。
- 可见性：/vmapp 0700 + /dev/vmapp 0600 + ioctl 需 CAP_SYS_ADMIN。
- 注册：oak_lsm.c 的 oak_init 调 openos_vmapp_init()（misc_register）；Makefile 加 vmapp.o。
- 用户态：`SCRIPT/libvmapp.{h,c}`（vmappapi 封装 ioctl）、`SCRIPT/vmapp_demo.c`（enter/exit/list/bad/lsroot 测试）。
- 文档：`VMAPP.md`。

## opt 统一包管理前端（用户态 Bash，已完成）
- 需求：首次自动装 apt + 询问装其他后端；动态后端源(GitHub/HTTP JSON)；克隆/补丁/构建/安装/注册；vmapp 集成；opt adapt 实验性。
- 文件：`SCRIPT/opt.sh`（opt_init/load_backends/install_backend/opt_call/opt_adapt + vmapp_enter/exit）、`SCRIPT/opt/backends.sources.example`、`SCRIPT/opt/index.example.json`（yum/dnf/zypper 示例）、`SCRIPT/opt/build.sh.template`、`OPT.md`。
- 依赖：jq/python3（JSON 解析）、curl、git、pacman（装 apt）。
- 说明：apt 在 Arch 需自举（pacman 无 apt 包）；FORCE 跳过依赖；日志 /var/log/opt-init.log。

## apt 内置到 opt（用户要求：opt 只内置 apt，不下载）
- 用户：opt 目前只内置 apt；把 apt 代码拉下来修改，内置进 opt，运行时不再下载。
- apt 源码：浅克隆 `https://github.com/Debian/apt.git`（注意：`apt-team/apt` 不存在，官方 mirror 是 **Debian/apt**）到 `OPENOS_CORE/src/apt`（26M，.git 已移除）。版本 3.1.12（main 分支）。
- **apt 是 CMake 项目**（非 autotools）。
- 适配（`src/apt/`）：
  - `CMakeLists.txt`：PACKAGE_VERSION → `3.1.12-openos1`
  - 新增 `vendor/openos/{apt-vendor.ent,openos.sources.in}`（OPENOS vendor：keyring=openos-archive-keyring、源 repo.openos.org/apt）
  - `vendor/getinfo`：优先检测 /etc/openos-release / /etc/openos_version → vendor=openos
  - `apt-pkg/deb/debsystem.cc`：检测到 openos-release/openos_version 时设 APT::Vendor=openos
  - `OPENOS-ADAPT.md`：适配说明文档
- `opt.sh` 改造：`install_builtin_apt()` —— apt 缺失时从本地 `src/apt` cmake 构建（-DWITH_DOC=OFF, prefix=/usr）→ sudo make install → 写 /etc/openos_version；**不下载不联网**。可选项无时提示"当前仅内置 apt"。
- 用户拒绝了我用 git init/commit 生成 openos.patch 的方式 → 改用手写文档记录适配点，构建直接用已修改源码（无需 patch）。
- 构建依赖：cmake zlib lz4 zstd libcurl libdb bz2 libgcrypt gettext g++（OPENOS 上 pacman 可装）。

## 应用启动器（应用抽屉，OPENUI-desktop 一部分，已完成）
- 用户：应用程序抽屉叫"应用程序启动器"，用 OPENUI 做，作为 OPENUI-desktop 的一部分。
- 实现：
  - `shellbackend.h/cpp`：新增 `AppsModel`（name/exec/vmapp 三角色）+ `ShellBackend::refreshApps()`（扫描宿主 /usr/share/applications、/usr/local/share/applications + vmapp 各环境 /vmapp/<app>/usr/share/applications，解析 Name/Exec）+ `launchInVmapp()`（隔离环境启动）；`apps` Q_PROPERTY。
  - `Launcher.qml` 重写：动态应用抽屉——ListView + shell.apps、分组（系统应用/虚拟环境: xxx）、搜索框（键盘优先）、文字符号图标、hover/pressed 状态层叠、点击 `launchInVmapp`。
  - `OpenUI.qml`：补排印令牌 typeTitle/typeBodyM/typeLabelL/M/S。
- 说明：vmapp 列表目前为原型枚举（opt/firefox/code），生产应经 libvmapp 枚举 /vmapp 子目录。
- lint 错误全为 macOS 缺 Qt 头，需 Linux 编译。

## OPENUI 视觉调整（圆角加大 + 毛玻璃更透 + 亲和，已完成）
- 用户要求：任务栏圆角更大；所有 GUI 圆角在 8px 基础上再加大；透明度再高一点（模糊透明而非直接透明）；让普通人更习惯。
- 修改：
  - `OpenUI.qml` + `openui.h`（C/QML 双端同步）：shapeXs 4→8、shapeSm 8→12、shapeMd 12→16、shapeLg 16→24；新增玻璃透明度令牌 glassPanelAlpha=0.72/glassCardAlpha=0.75/glassMenuAlpha=0.78（C 端 OUI_GLASS_*_ALPHA）；hover 状态增强 0.08→0.12、focus/pressed 0.12→0.15。
  - `Panel.qml`：面板 0.88→glassPanelAlpha(0.72)；新增底部 1px 阴影条（层次感）；任务栏按钮 radius shapeSm(12)+hover 高亮+Behavior 颜色动画；Menu 按钮圆角 shapeSm。
  - `Notifications.qml`：0.9→glassCardAlpha(0.75)，圆角 12。
  - `Launcher.qml`：0.92→glassMenuAlpha(0.78)，圆角 shapeLg(24)，项间距更宽松。
  - `OPENUI.md`：设计原则 4 改为"柔和圆角"+新增 6"亲和易用"；形状阶梯表更新 + 玻璃透明度说明。
- 合成器不画圆角（窗口边框直线 rect），无需改。
- lint 仍为 macOS 缺 Qt 头。

## 通知卡片 hover + 启动器项悬停色（已完成）
- `Notifications.qml`：卡片 hover 交互——悬停整体 scale 1.02（轻微上浮）、边框转 primary 色（0.6 alpha）、accent 竖条加宽 3→5px、Behavior 动画；MouseArea 加 id=hover + hoverEnabled。
- `Launcher.qml`：启动器项 hover 增强——左侧 primary 指示条淡入（3px）、图标与名称 hover 转 primary/onSurface 色（原为灰）、Behavior 颜色动画。
- 与任务栏按钮（已有 hover 高亮）三处交互反馈风格统一。

## 交互细节第二波（Menu hover + 胶囊 hover + 启动器动画，已完成）
- `Panel.qml`：工作区胶囊 hover（变宽 +6px、非激活时转 primary 0.6 alpha、width/color 动画）；Menu 按钮 hover（primary 0.8 alpha 提亮、打开时转 primaryContainer + onPrimaryContainer 文本、Behavior 动画）。
- `Launcher.qml`：打开动画——淡入（opacity 0→1）+ 缩放（0.96→1）OutCubic 200ms，用显式 ParallelAnimation + onVisibleChanged restart（Behavior 同步设置不可靠，改用显式动画对象）；transformOrigin TopRight。

## 内核文档更新（所有四内核，已完成）
- 新增 `Documentation/security/openos-security.rst`（四内核完全一致 md5 4403f102...）：OPENOS Security 模块完整文档（设计目标/模块文件/强制启用三层/钩子/子安全主体/授权提权/OAK-Seal/跨版本/配置/用户态配套）。
- 注册进四内核 `Documentation/security/index.rst` 的 toctree（末尾追加 `openos-security`）。
- 新增 `security/openos/README.md`（四内核）：文件表/强制启用/用户态接口(/proc/oak/*)/指向 RST；5.15 版 order 措辞单独修正为 LSM_ORDER_FIRST(唯一强制项)。
- 四内核 security/openos/ 结构统一：Kconfig Makefile README.md oak_early.c oak_lsm.c oak_rsa.c。

## 全套 UI 模块（用户"所有都做"，已完成）
- 新增 13 个 QML 组件（OPENUI-desktop/src/shell/qml/）：
  - P0：Tray（托盘 音量/WiFi/电池/蓝牙）、QuickSettings（快捷设置 滑条+开关）、PowerMenu（关机/重启/锁屏/注销）、LockScreen（大时钟+密码，对接 OAK 解锁）
  - P1：CommandPalette（Ctrl+K 命令面板 模糊搜索+键盘导航）、CalendarPopup（月历）、AltTabSwitcher（窗口切换）、RunDialog（Alt+F2）
  - P2：SettingsCenter+SettingsPages（设置中心 侧栏5类：外观/安全[OAK]/软件包[opt]/系统/网络）、OptManagerUI（opt 图形前端）、VmappManager（软件隔离管理）
  - P3：WelcomeScreen（3 步首次引导）、ThemeCustomizer（强调色/圆角/玻璃实时预览）
- 集成：main.qml 重写（全局 Shortcut：Ctrl+K/L/T/S/P/V、Alt+F2、Alt+Tab；overlayLayer 叠放；独立窗口；欢迎屏）；Panel.qml 加时钟弹日历+托盘+电源按钮+3 弹出组件；qml.qrc 注册 19 文件。
- 技巧：QML 不能直接改 Repeater modelData，开关类用 ListModel+ListElement；SettingsPages.qml 单独文件被 SettingsCenter 引用。
- qml lint 0 错误；运行时需 OPENOS/Linux（Qt6）。

## 独立 app 架构（用户"每个 app 单独构建"）
- 用户原则：OPENUI 只是 GUI 框架（不内置 app 功能）；需交互功能的都是独立 app。
- **7 个独立 app** 位于 `OPENOS_CORE/src/openos-*`（与 apt/bash 平级），每个**自包含、可单独 `meson setup` 构建**：
  - `openos-settings`（设置：main+Settings.qml+SettingsPages.qml+OpenUI.qml+qrc+meson，完整）
  - `openos-oak`（OAK 安全中心）
  - `openos-opt`（包管理，需 liboak）
  - `openos-vmapp`（软件隔离管理）
  - `openos-calendar` / `openos-run` / `openos-welcome`
- 每个 app 目录内容：`main.cpp`（加载 qrc:/qml/<App>.qml + OpenUI 令牌）+ 主 QML + `OpenUI.qml`（令牌副本，自包含不依赖外部）+ `qml.qrc` + `meson.build`（qt6.preprocess + qt6_dep，独立 project）。
- 删除 `OPENUI-desktop/src/apps/app_main.cpp`（残留）；`src/ui/openui_component.{h,cpp}` 保留（共享组件库，供 app 复用令牌）。
- OPENUI-desktop 回归纯 GUI 框架（合成器 + 外壳 + 令牌），不含 app。

## SCRIPT/ 目录废除（用户：不是 script，要么是 app 要么是内核功能）
- 用户明确指出 SCRIPT/ 里的都不是 script，应归类为 app 或内核功能。
- **已删除 SCRIPT/**，全部内容归类到 `OPENOS_CORE/src/` 下独立组件（与 apt/bash 平级，各带 meson.build 可独立构建）：
  - `src/openos-securityd/`（OAK 协议守护：openos-securityd.c + build-securityd.sh）
  - `src/openos-settingsd/`（设置守护：openos-settingsd.c + oak-test-client.c + build-oak-settings.sh）
  - `src/liboak/`（OAK Settings API 库：liboak.{c,h}，静态库）
  - `src/libvmapp/`（vmapp 用户态库：libvmapp.{c,h} + vmapp_demo）
  - `src/openos-oak-seal/`（封印工具：openos-oak-seal.c）
  - `src/oakctl/`（.oak 密钥工具：oakctl.sh）
  - `src/libopenrsa/`（OPEN RSA 参考实现：openrsa.{c,h} + openrsa_test + build-openrsa.sh；内核权威在 kernel oak_rsa.c）
  - `src/opt/`（CLI 前端：opt.sh + opt/ 配置模板）
- 每个组件补了 `meson.build`（C 用 dependency libsodium/openssl，bash 用 install_data）。
- **路径修正**：opt.sh OPT_APT_SRC 改为 `../apt`；liboak/settingsd 的 OAK-SK 默认路径改为绝对 `/etc/openos/security/oak-sk.key`（原 `../KEYS/.private/oak-sk.key` 相对 SCRIPT/ 已失效）。
- **文档引用更新**：OAK-SETTINGS-API.md/VMAPP.md/SECURITY-OAK.md/OPT.md + 四内核 README 中 SCRIPT/ → src/ 对应路径（sed 批量替换）。
- src/ 现状：22 个组件全部带 meson.build 可独立构建；顶层仅 BUILD_SYSTEM/ KEYS/ LICENSES/ src/。

## 构建系统 Soong 化 + 设备专属密钥 + 文档/许可归档（已完成）
- **Soong 风格构建**：借鉴谷歌 Soong/Blueprint 但**用 OPENOS 自己的名字**（用户指出没必要用谷歌名）：
  - `BUILD_SYSTEM/OPENOS.bp`（原 Android.bp，27 模块声明）+ `BUILD_SYSTEM/openos-build`（Python 驱动，原 soong）。
  - `.bp` = Blueprint 格式（谷歌声明式构建描述），OPENOS 改名 OPENOS.bp。
- **设备专属密钥**（用户：KEYS 文件夹不对，每台设备内核要自生成设备专属 key；用户已删 KEYS/）：
  - 新增 `kernel/*/security/openos/oak_devicekey.c`（四内核一致 md5 013e56...，Makefile 加 oak_devicekey.o）。
  - 首次启动内核自动生成 RSA-2048 设备密钥对：公钥经 /proc/oak/device-key 导出，私钥经安全通道交用户态存 /etc/openos/security/device.key；每设备唯一不预置。
  - 接入 oak_lsm.c 的 oak_init。
- **DOCS 文件夹**：新建 `DOCS/`（README 索引 + SOURCE-GUIDE.md 源码库指南 + KERNEL/ + SECURITY/ + PACKAGING/ + COMPONENTS/ + BUILD/），从根目录移入 OAK-SETTINGS-API/OPT/VMAPP/SECURITY-OAK 文档，归档内核 openos-security.rst、apt 适配、OPENUI、构建文档。
- **LICENSES 归档**：`LICENSES/`（README + OPENOS-LICENSE.md[自研 GPL-2.0] + components/<组件>/ 各上游许可副本），自研组件补 COPYING。
- **文档路径修正**：SOURCE-GUIDE/SECURITY-OAK 移除 KEYS/ 引用，改设备密钥机制；OAK-SK 路径改 /etc/openos/security/oak-sk.key。
- 用户取消了我执行 grep 检查 KEYS 残留的命令，未重试；未执行命令验证。

## 用户问题解答（本次对话中）
- uname -r 返回值：由内核 Makefile VERSION.PATCHLEVEL.SUBLEVEL 决定——6.12.103→"6.12.103"、7.1.8→"7.1.8"、7.2-rc7→"7.2.0-rc7"、5.15.215→"5.15.215"；macOS 上跑 uname -r 返回 macOS 版本，需在 OPENOS 上验证。
- Android.bp 含义：.bp=Blueprint 格式（谷歌声明式构建描述文件，类似 CMakeLists.txt），Android=用于 Android 的 Blueprint；已改名为 OPENOS.bp。

## openos-uname（ASCII logo + 系统信息，已完成）
- 需求：输入 uname 回复一个类似 ASCII 字符图像（neofetch 风格）。
- `src/openos-uname/openos-uname.sh`：无参数输出 OPENOS ASCII art logo（"OPENOS" 大字）+ 系统信息（OPENOS 版本/内核/架构/主机）；带标准 uname 选项（-a -s -n -r -v -m -p -i -o）时兼容标准 uname。
- `meson.build`：独立可构建，安装为 openos-uname（注释给出可替换为 uname 的方式）。
- `OPENOS.bp` 加 tool_module openos-uname。
- 已验证：bash -n 语法 OK，模拟输出完整 logo + 信息（本机已实测）。

## uname 系统内置 OPENOS 化（已完成）
- 用户要求：直接在系统层面内置（不是单独工具）；或让 uname -r 输出 "openos 2026.1.0+DEV+{内核版本}"。
- **内核版本字符串**：四内核 Makefile EXTRAVERSION 改为 `-openos2026.1.0+DEV`（5.15/6.12/7.1）与 `-rc7-openos2026.1.0+DEV`（7.2-rc7）→ uname -r 输出 `6.12.103-openos2026.1.0+DEV` 等。注意：macOS sed/perl 对 EXTRAVERSION 行替换需用 `s/^EXTRAVERSION =$/.../`（精确锚定，之前 perl \n 替换失败）。
- **coreutils uname 源码内置 logo**：解压 coreutils-9.11.tar.gz 到 src/，修改 uname.c（真实路径含 src/coreutils-9.11/）：
  - 加 `-O`/`--openos` 选项（uname_long_options + getopt "asnrvmpioO" + case 'O' + PRINT_OPENOS_LOGO=256）
  - 新增 print_openos() 函数（ASCII logo + OPENOS DEV2026.1/内核/架构/主机）
  - main 中 `if (toprint & PRINT_OPENOS_LOGO)` 调用并返回
  - 修正 bug：`name.sysname " " name.release` 字符串拼接非法 → 改 `printf("%s %s", name.sysname, name.release)`
- 验证：最小 C 测试编译 OK（logo + 系统信息显示正常）；uname.c 全量编译仅缺 gnulib 头（configure 后才有），语法无误。
- 遗留：coreutils 已解压（src/coreutils-9.11/），构建直接用该目录。

## uname 默认显示 OPENOS logo（已完成）
- 用户要求：`-O`/logo 设为默认（无参数时 uname 直接显示 OPENOS logo）。
- `coreutils-9.11/src/uname.c` main 中默认逻辑改为：
  `if (toprint == 0 && uname_mode == UNAME_UNAME) toprint = PRINT_OPENOS_LOGO;`
  （uname 模式无参数默认 logo；arch 模式仍是 machine）。
- 帮助文本 -O 行插入失败（perl/sed 对 `\n\` 转义匹配问题），不影响功能，可后续补。
- 语法验证：仅缺 gnulib attribute.h（configure 生成），代码无错误。
- 效果：OPENOS 上直接 `uname` → OPENOS ASCII logo + 系统信息；`uname -r` → `6.12.103-openos2026.1.0+DEV`。

## LoongArch64 支持 + 远程仓库对齐（B 方案，已完成）
- 用户要求：加 LoongArch64；用 B 方案（根据本地实际实现更新远程 README）。
- **LoongArch64 加入构建体系**：
  - config.sh：OPENOS_TRIPLET[loong64]=loongarch64-openos-linux-gnu、OPENOS_KBUILD_ARCH[loong64]=loongarch、OPENOS_CROSS[loong64]=loongarch64-linux-gnu-、OPENOS_CROSS_PKG[loong64]=gcc-loongarch64-linux-gnu
  - setup_board.sh / build_component.sh 的 --all 循环加 loong64
  - .github/workflows/build.yml matrix 加 loong64（5 架构）
  - 内核 6.12/7.1 已原生支持 arch/loongarch（实测确认）
- **远程仓库**：https://github.com/OPENOS-dev/OPENOS（6 提交，README 为愿景描述）。对照本地：OAK/OPENUI/OPT/SNI(=vmapp)/跨平台 全部已在本地实现。
- **B 方案落地**：在 `OPENOS_CORE/README.md` 写了一份反映本地实际实现的完整 README（核心特性+实现状态 ✅、源码结构、组件一览、文档、许可证），供用户推送到 GitHub 替换远程愿景 README。
- 注意：本地 README 用了 OPL 许可表述（对齐远程），但本地 LICENSES 自研代码是 GPL-2.0-only——README 已注明两者。
- 用户未要求推送 git；本地仍无 .git（防误推）。

## kernel 子模块提交（LFS 方案废弃，改提交解压目录）
- **用户决策修正**：不需要提交压缩包，只需要提交解压目录（弃用之前的 Git LFS 跟踪 tarball 方案）。
- `src/kernel/` 子模块（远程 `https://github.com/OPENOS-dev/OPENOS-kernel.git`，分支 main）：
  - 新增 `.gitignore`：排除 `*.tar` / `*.tar.*`（tarball 不提交）与 `.DS_Store`。
  - `git add -A` 暂存 **343,469 个文件**（纯解压源码树，无构建产物 .o/.ko、无 >100M 单文件，直接提交不超 GitHub 限制）。
  - 提交 `2219c1e` "Import Linux kernel source trees (5.15.215 / 6.12.103 / 7.1.8 / 7.2-rc7)"。
- 主仓库：`git rm --cached src/kernel` 清理旧状态 → 手写 `.gitmodules`（path=src/kernel, url=OPENOS-kernel.git）→ `git add .gitmodules src/kernel` 挂为 gitlink（mode `160000`）。
- **用户决策（2026-08-16 追加）**：`src/` 下**所有**模块都作为子模块（不只大组件），共 26 个待建：A 上游 9（gcc/glibc/systemd/coreutils/util-linux/apt/pacman/bash/linux）、B 库工具 6（liboak/libopenrsa/libvmapp/oakctl/openos-oak-seal/openos-uname）、C app/服务 9（settings/calendar/welcome/vmapp/settingsd/securityd/run/opt/oak）、D 其他 2（OPENUI-desktop/opt）。命名统一 `OPENOS-<目录名>`，用户去创建仓库。
- 待办：26 个子模块逐个 git init + 排除 tarball + 提交 + 挂主仓库 .gitmodules（gcc/util-linux/coreutils 树内小压缩包为源码数据，保留）；主仓库首次提交推送需用户确认认证。
